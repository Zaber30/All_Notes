**Flatpak** is a **universal application packaging and distribution system for Linux**.

It allows developers to package an application with its required libraries and run it on many Linux distributions.